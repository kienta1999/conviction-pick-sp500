# Python
__pycache__/
*.pyc
.venv/
.python-version

# Data caches (regenerated by scripts/fetch.py)
data/

# output/<mode>/ artifacts (shortlists, funnel, dossiers, picks, parts) are
# COMMITTED deliberately — they're the audit trail next to each pick in
# picks/ledger.csv. parts/ holds the per-subagent work (triage, research
# batches, ballots, verification) behind each consolidated dossier.

# Playwright screenshot debris from UI sessions
.playwright-mcp/
dash-top.png

# IBKR web-trade session state (account snapshot / order file) — never commit
reports/web_*.json
